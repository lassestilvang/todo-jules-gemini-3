💡 What: Updated the TaskItem priority badge to use distinct variants for all priority levels (destructive for high, secondary for medium, and outline for low).
🎯 Why: Previously, 'low' and 'medium' priorities both used the 'secondary' variant, making them visually indistinguishable at a glance. Using distinct standard variants makes the list easier to scan.
📸 Before/After: Visual distinction added to priority badges.
♿ Accessibility: Preserved existing sr-only labels.
